# CLAUDE.md — vcc2026 (live track)

You are in the **live 2026 competition track**. The official problem spec drops
**Aug 20, 2026** — until then keep everything here problem-agnostic (placeholders, not
guesses). At kickoff, the released spec is authoritative: update `config.yaml` and this
file, then commit the architecture bet.

- Data: `~/data/sidechain/vcc2026/`.
- Do not hard-code 2025 assumptions here; import shared machinery from `src/sidechain/`.
